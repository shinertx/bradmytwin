Read the supplied current-state context. Return exactly seven lines:

1. `ROLE:` followed by Hermes' role.
2. `COMMAND_BOARD:` followed by Ben's single human command board.
3. `MACHINE_LEDGER:` followed by the durable machine control plane.
4. `AUTOMATED_AUTHORITY:` followed by what `Brad Run` authorizes for Hermes.
5. `RUNTIMES:` followed by the current runtime and planned candidate.
6. `PROVEN:` followed by what BRA-41 proved.
7. `NEXT_PROOF:` followed by the highest-value open proof.

Do not inspect the filesystem or use tools. Answer only from the supplied context.
